<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ischoolbar.programmer.dao.home.AccimageDao">
	<!-- 点击提交 -->
	<insert id="add" parameterType="com.ischoolbar.programmer.entity.home.Accimage">
		insert into accimage(id,accountid,select) values(null,#{id},#{select})
	</insert>
</mapper>