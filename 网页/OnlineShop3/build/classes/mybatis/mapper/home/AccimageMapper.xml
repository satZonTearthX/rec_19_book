<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ischoolbar.programmer.dao.home.AccimageDao">
	<!-- 点击提交 -->
	<insert id="add" parameterType="java.util.Map">
		insert into accimage(id,accountId,tags) values(null,#{accid},#{acctags})
	</insert>
</mapper>